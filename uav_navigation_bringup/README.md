# uav_navigation_bringup

Architecture-review skeleton for launch, native ROS parameters and RViz.

The launch files default to `architecture_only=true`; they intentionally do not
start the planned C++ nodes before mentor approval and implementation.
